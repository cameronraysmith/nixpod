name: Update Flake Inputs

on:
  schedule:
    # Tue+Fri 22 UTC / 5p EST / 6p EDT
    # revert to `0 3,15 * * *` for twice-daily sync with upstream updates
    - cron: "0 22 * * 2,5"
  workflow_dispatch:
    inputs:
      auto_merge:
        description: "Enable auto-merge for created PRs"
        required: false
        type: boolean
        default: false
      exclude_patterns:
        description: "Comma-separated exclude patterns (e.g., '**/flake.nix#nixpkgs')"
        required: false
        type: string
        default: ""
  workflow_call:
    inputs:
      auto_merge:
        description: "Enable auto-merge for created PRs"
        required: false
        type: boolean
        default: false
      exclude_patterns:
        description: "Comma-separated exclude patterns"
        required: false
        type: string
        default: ""
    secrets:
      FLAKE_UPDATER_APP_ID:
        required: true
      FLAKE_UPDATER_PRIVATE_KEY:
        required: true

concurrency:
  group: update-flake-inputs-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

jobs:
  update-flake-inputs:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Generate GitHub App Token
        id: app-token
        uses: actions/create-github-app-token@1b10c78c7865c340bc4f6099eb2f838309f1e8c3 # v3
        with:
          app-id: ${{ secrets.FLAKE_UPDATER_APP_ID }}
          private-key: ${{ secrets.FLAKE_UPDATER_PRIVATE_KEY }}

      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # ratchet:actions/checkout@v6
        with:
          token: ${{ steps.app-token.outputs.token }}
          fetch-depth: 0

      - name: Setup Nix
        uses: ./.github/actions/setup-nix
        with:
          installer: quick
          system: x86_64-linux

      - name: Update flake inputs
        uses: mic92/update-flake-inputs@1e056167bd2101e587bd39deb2f64909b4759652 # ratchet:mic92/update-flake-inputs@v1
        with:
          github-token: ${{ steps.app-token.outputs.token }}
          exclude-patterns: ${{ inputs.exclude_patterns }}
          pr-labels: "flake-update"
          auto-merge: ${{ inputs.auto_merge && 'true' || 'false' }}
          # Upstream bug: delete-branch uses maintainer_can_modify API which only
          # works for fork PRs. Use repo setting "Automatically delete head branches"
          delete-branch: "false"
          signoff: "true"
